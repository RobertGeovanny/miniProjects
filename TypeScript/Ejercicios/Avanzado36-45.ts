// 36.Crea una función que tome un Generic<T> y lo transforme.

// 37.Crea una función que reciba una callback tipada.

// 38.Escribe una clase Stack<T> (pila) genérica con push, pop y peek.

// 39.Modela un sistema de tareas con estados (pending, done, archived) y tipado fuerte.

// 40.Usa Mapped Types para transformar tipos (T a Readonly<T>, por ejemplo).

// 41.Simula una API con una interfaz de respuesta genérica (ApiResponse<T>).

// 42.Crea tipos condicionales simples (T extends string ? X : Y).

// 43.Usa infer para extraer el tipo de retorno de una función.

// 44.Modela un formulario con validaciones tipadas.

// 45.Crea una función que reciba múltiples parámetros tipados y devuelva un objeto.